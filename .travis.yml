os:
  linux

dist:
  trusty

language: node_js
node_js:
  - lts/*

script:
  - npm run docs:build

branches:
  only:
    - develop

git:
  quiet: true

deploy:
  provider: pages:git
  token: $github_token
  edge: true
  cleanup: false
  keep_history: true
  local_dir: docs/.vuepress/dist
  target_branch: master